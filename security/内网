function getInformation(){
    var value = document.getElementById('BookIDIn').value
    let num = 1;
    while(num)
        if (value === "我什么都做不到"){
            document.location.href="inner.html";
            num=0;
        }
        else{
            alert("重来");
            break;
        }
}
